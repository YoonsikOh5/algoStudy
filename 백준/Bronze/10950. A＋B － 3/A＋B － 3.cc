#include <bits/stdc++.h>

using namespace std;

void plusab(int a, int b) {
    
    cout << a + b << endl;
}

int main() {

    int T;
    cin >> T;

    for (int i = 0; i < T; i++) {
        int a, b;
        cin >> a >> b;
        plusab(a, b);
    }

}